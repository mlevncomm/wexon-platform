import type { ReactNode } from "react";

/** Route group pass-through — operasyon layout root layout.tsx tarafından sağlanır. */
export default function WexPayPanelLayout({ children }: { children: ReactNode }) {
  return children;
}
